{
  "name": "digi-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "build:frontend": "cd frontend && npm ci && npm run build",
    "build": "npm run build:frontend",
    "start": "node server.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
